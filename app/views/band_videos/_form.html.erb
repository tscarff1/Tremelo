        <%= render partial: 'shared/error_messages', locals: {obj: @band_video} %>
          <h3>Add a New Video</h3>

          <div class="form-group">
            <div class="col-xs-8 col-md-8">
              <%= form.label "Video Name", class: "control-label" %>
              <%= form.text_field :video_name, class: "form-control",
                  required: "required" %>
            </div>
          </div>

          <div class="form-group">
            <div class="col-md-10">
              <%= form.label "Enter the link to your video" %>
              <%= form.text_field :video_link, class: "form-control", 
                  required: "required" %>
            </div>
          </div>

      <div class="form-group">
        <div class="col-md-12">
          <%= form.submit "Submit Changes", class: "btn btn-block" %>
        </div>
      </div>
