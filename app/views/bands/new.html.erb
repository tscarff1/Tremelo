<%= @band.current_signup_step%><br>

<%= @band.signup_params%><br>

<%= form_tag searches_path, method: :get do %>
<%= render partial: "#{@band.current_signup_step}_step"%>
<br>
<br>
<br>
<%= submit_tag "Previous", name: "back" unless @band.first_step? %>
<%= submit_tag "Next" unless @band.last_step? %>
<br>
<br>
<% end%>